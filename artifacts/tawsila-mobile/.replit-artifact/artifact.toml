kind = "mobile"
previewPath = "/mobile/"
title = "Tawsila Mobile"
version = "1.0.0"
id = "artifacts/tawsila-mobile"
router = "expo-domain"

[[integratedSkills]]
name = "expo"
version = "1.0.0"

[[services]]
ensurePreviewReachable = "/status"
name = "expo"
paths = [ "/mobile/" ]
localPort = 25896

[services.development]
run = "pnpm --filter @workspace/tawsila-mobile run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/tawsila-mobile", "run", "build" ]
run = [ "pnpm", "--filter", "@workspace/tawsila-mobile", "run", "serve" ]

[services.env]
PORT = "25896"
BASE_PATH = "/mobile/"
